{
  "name": "ai-class-scheduler-with-functions",
  "version": "1.0.0",
  "description": "An intelligent web application to help school administrators assign classes to classrooms without conflicts.",
  "scripts": {
    "build": "mkdir -p dist && cp index.html metadata.json dist/ && esbuild index.tsx --bundle --outfile=dist/index.js --platform=browser --format=esm --jsx=automatic --external:react --external:react-dom/* --external:jspdf --external:jspdf-autotable"
  },
  "dependencies": {},
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
